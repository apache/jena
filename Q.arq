PREFIX : <http://people.example/>
SELECT ?y ?name
WHERE
{
  {
    SELECT ?y ?name
    WHERE
    {
      {
        SELECT ?y ?name
        WHERE 
        {
          :alice :knows ?y .
          ?y :name ?name .
        }
      }
    }
  }
}
