PREFIX : <http://example/>
PREFIX  dc:     <http://purl.org/dc/elements/1.1/>

SELECT ?p ?q ?count # COUNT(?x)
{
    ?x :r ?r .
    OPTIONAL { ?x :p ?p . }
    OPTIONAL { ?x :q ?q . }
}
GROUP BY ?p ?q
# HAVING (?count > 1)
ORDER BY ?q ?count
