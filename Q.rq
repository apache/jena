PREFIX : <http://example/>

SELECT *
WHERE {
    {
        SELECT ?x ((COUNT(?y)) AS ?c)
        WHERE {
            ?x :p 123 .
            OPTIONAL {
                ?inst a ?x .
            } .
        }
        GROUP BY ?x
    } .
}
