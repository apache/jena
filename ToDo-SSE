==== SSE

+ Builder tidying and testing

+ Expr: (exists (pattern ...))
  
+ (group (vars) aggregate)
  
+  Assignment:
  (assign ((?x expr) (?y expr)) --  (:= ?x expr) [is this SELECT/project?]
     (project ...))
     (project (?x ?y (?z expr)) ...)
  
+ IRI/PrefixedName => Node : make this String (no - allows for _:abc)
  Applies through out parser system.