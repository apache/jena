==== SSE

+ test another form -- (debug FOO)

+ Expr: (exists (pattern ...))
  
+ (group (vars) aggregate)
  
+  Assignment:
  (assign ((?x expr) (?y expr)) --  (:= ?x expr) [is this SELECT/project?]
     (project ...))
     (project (?x ?y (?z expr)) ...)
