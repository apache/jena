PREFIX foaf:       <http://xmlns.com/foaf/0.1/>
PREFIX :           <http://example/>

SELECT * {
  ?s a foaf:Person .
  OPTIONAL { ?s :hairColor ?color . FILTER(?color = "brown") }
  FILTER(!bound(?color))
} 