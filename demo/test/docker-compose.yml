# Licensed under the terms of http://www.apache.org/licenses/LICENSE-2.0
services:
  fuseki:
    image: ${IMAGE_NAME:-fuseki-ai}:${IMAGE_TAG:-6.1.0-SNAPSHOT}
    ports:
      - "3030:3030"
    volumes:
      - ./config.ttl:/fuseki/config.ttl:ro
      - fuseki-db:/fuseki/DB
      - fuseki-lucene:/fuseki/Lucene

volumes:
  fuseki-db:
  fuseki-lucene:
